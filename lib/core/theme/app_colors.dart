import 'package:flutter/material.dart';

class AppColors {
  /// Primary brand color
  static const Color primary = Color(0xff377DFF);

  /// Text and icon default color
  static const Color textPrimary = Color(0xff243443);

  /// Hint text color (e.g., input placeholders)
  static const Color hint = Color(0xffAAB0B7);

  /// Canvas/background for certain components
  // static const Color canvas = Colors.red; //Color(0xff58616A);

  /// Backgrounds for scaffold, app bar, etc.
  static const Color white = Colors.white;

  /// Transparent surface for Material 3 app bars
  static const Color transparent = Colors.transparent;

  /// Grey color for item borders and other elements
  static const Color grey = Color(0xff58616A);
}
